/**
 * Supabase configuration template.
 *
 * Copy this file to `supabase-config.local.js`, fill in the public project
 * values there, and never commit the local file to Git.
 */
window.STUDY_TOOLS_SUPABASE_CONFIG = {
  enabled: false,
  url: "",
  anonKey: "",
  auth: {
    persistSession: true,
    autoRefreshToken: true,
    detectSessionInUrl: true
  }
};
